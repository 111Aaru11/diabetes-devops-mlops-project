apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
  name: diabetes-monitor
  labels:
    release: monitoring

spec:
  selector:
    matchLabels:
      app: diabetes-app

  endpoints:
    - port: http
      path: /metrics
      interval: 5s